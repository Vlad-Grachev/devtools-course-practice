#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_can_Run
    COMMAND ${APPLICATION}
)
set_tests_properties (${prefix}_can_Run PROPERTIES
    LABELS "${MODULE}")

add_test(
    NAME ${prifix}_First_Simple_Test
    COMMAND ${APPLICATION} 1
)
set_tests_properties (${prefix}_First_Simple_Test
  PROPERTIES PASS_REGULAR_EXPRESSION "1")
